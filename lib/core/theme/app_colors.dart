import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary Background (Warm Beige)
  static const Color background = Color(0xFFE8E4DF);

  // Card / Container Background (Pure White)
  static const Color cardBg = Color(0xFFFFFFFF);

  // Primary Text (Charcoal Black)
  static const Color textPrimary = Color(0xFF2E2E2E);

  // Secondary Text (Warm Grey)
  static const Color textSecondary = Color(0xFF6F6B66);

  // Pure Black Accent / Core Icons / Inputs
  static const Color pureBlack = Color(0xFF111111);

  // Soft Heart Red/Rose Accent
  static const Color accentRed = Color(0xFFC05C5C);

  // Soft shadow color
  static const Color shadow = Color(0x0F000000);

  // Subtle border line
  static const Color divider = Color(0x1F6F6B66);
}
